name: google_map_fade_markers
description: "This package integrate for the markers a fade animation when the
  markers changes"
version: 0.0.6
repository: https://github.com/JoseCaro01/google_map_fade_markers

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.17.0

dev_dependencies:
  build_runner: ^2.4.8
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5

flutter: 
